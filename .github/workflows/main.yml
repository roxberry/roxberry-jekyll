name: deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - deployment
    paths:
      - 'dist/**'

jobs:
  run-deploy:
    runs-on: ubuntu-latest
    container: jekyll/jekyll:4.0.0
    steps:
      # Checkout
      - name: Checkout Code
        uses: actions/checkout@master
      # Note the following may not be required in future versions of Jekyll Build Actions
      - name: Make build destination directory
        run: |
        # mkdir -vp ~/dist
        chmod -R 777 /__w/roxberry/roxberry
      # Jekyll  
      - name: Get Jekyll Dependencies
        run: |
          gem install bundler jekyll
          bundle install --jobs 4 --retry 3
      # Grunt
      - name: Get Grunt Dependencies
        run: |
          npm install
      # Build
      - name: Grunt Build and Copy
        run: 
          npm run build
        
        